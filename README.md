files / projects / scripts etc assosciated with lab work that are not refined / proper / valid enough to push to a open repository
commandscript.ipynb must be opened and run in jupyter lab/notebook: run the commands 
pip install jupyterlab, jupyter lab to open jupyter. imports are handled in the code so, theres no point in setting up an environment except to be out of system32
